# opencode_compat_proxy
 A FastAPI proxy that translates non-standard tool call formats (DeepSeek DSML, Qwen XML) from LLM backends into OpenAI-compatible tool_calls structure, enabling OpenCode to work with models that output raw markup instead of structured JSON
